import 'package:flutter/material.dart';

class TransactionStatment extends StatelessWidget {
  const TransactionStatment({super.key});
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      
      body: Center(
        child: Text('Welcome to Transaction Statment'),
      ),
    );
  }
}