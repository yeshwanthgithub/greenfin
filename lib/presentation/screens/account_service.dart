import 'package:flutter/material.dart';

class AccountServiceWidget extends StatelessWidget {
  const AccountServiceWidget({super.key});
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Welcome to Account Services'),
    );
  }
}
