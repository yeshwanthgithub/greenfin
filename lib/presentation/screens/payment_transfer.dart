import 'package:flutter/material.dart';

class PaymentTransfer extends StatelessWidget {
  const PaymentTransfer({super.key});
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Welcome to Payment Transfer'),
    );
  }
}
